export interface Teacher {
  id: number;
  userId: number;
  firstName: string;
  lastName: string;
  email: string;
  phone?: string;
  hireDate: string;
  createdAt?: string;
  updatedAt?: string;
}
